# Configuración del servidor
#server:
#  port: 8082

# Configuración de la aplicación Spring
spring:
  application:
    name: customer-ms

  mvc:
    throw-exception-if-no-handler-found: true

  # Configuración de la base de datos
  datasource:
    url: jdbc:mysql://autorack.proxy.rlwy.net:25967/railway
    username: root
    password: WzvqTyAqnqQGihPaPjtWDtJBtoRfWKbz
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# Configuración de SpringDoc para OpenAPI y Swagger UI
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    url: /openapi.yaml



# Configuración de Eureka
eureka:
  client:
    service-url:
      defaultZone: https://euraka-server-production.up.railway.app

  instance:
    prefer-ip-address: true
